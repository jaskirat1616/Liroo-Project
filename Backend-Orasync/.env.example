# Google Generative AI (Gemini) API Key
# Get your API key from: https://makersuite.google.com/app/apikey
GENAI_API_KEY="YOUR_GOOGLE_GENAI_API_KEY"

# Backend Base URL
# For local development: http://localhost:5000
# For production: https://your-backend-url.run.app
BASE_URL="http://localhost:5000"

# Google Cloud Storage Bucket Name
# Format: your-bucket-name.appspot.com or your-bucket-name
GCS_BUCKET_NAME="your-gcs-bucket-name"

# Google Cloud Service Account Credentials Path
# Path to your Firebase/Google Cloud service account JSON key file
# For local development: /path/to/firebase-service-account.json
# For Cloud Run: Leave empty (uses default credentials)
GOOGLE_APPLICATION_CREDENTIALS="/path/to/firebase-service-account.json"

# Optional: Port for local development
# Default: 8080 (for Cloud Run) or 5000 (for Flask dev server)
PORT=5000
